{{ openresty_log_dir }}/*.log {
  daily
  compress
  delaycompress
  rotate {{ openresty_logrotate_rotate }}
  missingok
  nocreate
  sharedscripts
  postrotate
    test ! -f {{ openresty_pid_file }} || kill -USR1 `cat {{ openresty_pid_file }}`
  endscript
}